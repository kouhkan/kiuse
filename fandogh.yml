kind: ExternalService
name: django-kiuse
spec:
  image_pull_policy: Always
  port: 80
  source:
    context: .
    media_path: ''
    project_type: django
    python_version: '3.10'
    static_path: static
    wsgi: config.wsgi
